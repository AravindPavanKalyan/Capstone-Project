import { emailValidator } from './app-validators';

describe('emailValidator', () => {
  // testing whether an instance is created
  it('should create an instance for emailValidator', () => {
    expect(emailValidator).toBeTruthy();
  });
});
